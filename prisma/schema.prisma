generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Actor {
  tmdbId        Int       @id
  numberOfVotes Int
  lastVotedAt   DateTime?
}
